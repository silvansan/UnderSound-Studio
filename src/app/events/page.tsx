import { Layout } from '@/components/Layout'

export default function EventsPage() {
  return (
    <Layout title="Events">
      <p style={{ color: 'var(--us-muted)' }}>
        Event listing will load from Payload in a later phase. Create events in the admin panel.
      </p>
    </Layout>
  )
}
